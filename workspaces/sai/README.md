# Sai workspace

Use this directory for disposable profiling, surrogate, and calibration experiments.
Read frozen inputs from `proto_programs/generated/`, promote stable code into
`src/protofuse/sai/`, and keep traces/datasets/weights under ignored `data/` paths.
