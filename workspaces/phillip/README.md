# Phillip workspace

Use this directory for Phillip's disposable end-to-end marimo notebooks and
experiments. Promote stable, tested code into `src/protofuse/phillip/` or a shared
package; do not commit large paper files or generated runs.

Philip-only agent instructions live in `AGENTS.local.md` in this directory. That file
is gitignored and stays local to this machine.
