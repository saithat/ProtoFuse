# Copy this file to .env. Never commit real credentials.
ANTHROPIC_API_KEY=
PAPERCLIP_API_KEY=
HF_TOKEN=

# Optional. Proto otherwise uses ~/.proto.
PROTO_HOME=
PROTO_MODEL_CACHE=

# Keep the default lightweight model replaceable without code changes.
PROTOFUSE_ANTHROPIC_MODEL=claude-sonnet-4-6
